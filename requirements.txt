# PiCam V2 — Python dependencies
# System packages (install via apt on Raspberry Pi OS):
#   sudo apt install python3-picamera2 python3-pil python3-pil.imagetk python3-piexif

# Pure-Python dependencies (pip-installable on all platforms)
Pillow>=10.0.0
piexif>=1.1.3
rawpy>=0.18.0

# Development / test
pytest>=7.4.0
pytest-mock>=3.11.0
